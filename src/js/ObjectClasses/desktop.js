class desktop {
//**
    * Builds a constructor for the desktop Variables
    * @param {int} sizeX 
    * @param {int} sizeY 
    * @param {String} Backround 
    * /
  constructor (sizeX, sizeY, Backround) {
    this.sizeX = sizeX
    this.sizeY = sizeY
    Backround = this.Backround
  }
}
